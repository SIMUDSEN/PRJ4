#include <iostream>
#include "spi.h"

using namespace std;

int main() {
    // Your code here
    cout << "initializing spi" << endl;
    /*
    initialize_spi();
    cout << "configuring general" << endl;
    config_general();
    cout << "Starting measurement" << endl;
    start_measurement();
    cout << "Getting data" << endl;
    struct measurement_total data = get_data();
    cout << "Data length: " << data.length << endl;

    for (size_t i = 0; i < data.length; i++)
    {
        cout << endl << endl << endl;
        cout << "Measurement " << i << endl;
        cout << "Angle: " << data.measurements[i].angle << endl;
        cout << "Dist 0: " << data.measurements[i].dist_0 << endl;
        cout << "Dist 1: " << data.measurements[i].dist_1 << endl;
        cout << "Dist 2: " << data.measurements[i].dist_2 << endl;
        cout << "Dist 3: " << data.measurements[i].dist_3 << endl;
        cout << "Dist 4: " << data.measurements[i].dist_4 << endl;
        cout << "Dist 5: " << data.measurements[i].dist_5 << endl;
        cout << "Dist 6: " << data.measurements[i].dist_6 << endl;
        cout << "Dist 7: " << data.measurements[i].dist_7 << endl;
        cout << "Temp 0: " << data.measurements[i].temp_0 << endl;
        cout << "Temp 1: " << data.measurements[i].temp_1 << endl;
        cout << "Temp 2: " << data.measurements[i].temp_2 << endl;
        cout << "Temp 3: " << data.measurements[i].temp_3 << endl;
        cout << "Temp 4: " << data.measurements[i].temp_4 << endl;
        cout << "Temp 5: " << data.measurements[i].temp_5 << endl;
        cout << "Temp 6: " << data.measurements[i].temp_6 << endl;
        cout << "Temp 7: " << data.measurements[i].temp_7 << endl;
    }
    */
    
    return 0;
}